export interface ReleaseInfo {
  id: number;
  title: string;
  dateTime: Date;
  version: string;
  fusionVersion: string;
}

export interface ReleaseInfoVersions {
  version: string;
  fusionVersion: string;
}
